# Leave blank in local dev — the CRA proxy (package.json "proxy") forwards
# /api/* to localhost:8000 automatically.
# In production (Vercel), set this to your Render backend URL, e.g.:
#   REACT_APP_API_URL=https://your-app.onrender.com
REACT_APP_API_URL=
REACT_APP_STRIPE_PUBLIC_KEY=pk_test_123abc
